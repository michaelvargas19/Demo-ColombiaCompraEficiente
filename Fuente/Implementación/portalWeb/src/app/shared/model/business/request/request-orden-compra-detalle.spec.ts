import { RequestOrdenCompraDetalle } from './request-orden-compra-detalle';

describe('RequestOrdenCompraDetalle', () => {
  it('should create an instance', () => {
    expect(new RequestOrdenCompraDetalle()).toBeTruthy();
  });
});
