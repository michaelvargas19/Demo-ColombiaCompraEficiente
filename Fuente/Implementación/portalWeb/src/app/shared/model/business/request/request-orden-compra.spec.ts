import { RequestOrdenCompra } from './request-orden-compra';

describe('RequestOrdenCompra', () => {
  it('should create an instance', () => {
    expect(new RequestOrdenCompra()).toBeTruthy();
  });
});
